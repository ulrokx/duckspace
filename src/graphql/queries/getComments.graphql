query GetComments($postId: String!) {
    getComments(postId: $postId) {
        ...CommentResp
        children {
            ...CommentResp
            children {
                ...CommentResp
                children {
                    ...CommentResp
                    children {
                        ...CommentResp
                    }
                }
            }
        }
    }
}
