query Post($id: String!) {
    post (id: $id) {
        id
        createdAt
        updatedAt
        voteStatus
        title
        text
        creator {
            id
            username
        }
        points
    }
}