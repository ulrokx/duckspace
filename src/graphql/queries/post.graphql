query Post($id: Int!) {
    post (id: $id) {
        id
        createdAt
        updatedAt
        voteStatus
        title
        text
        creator {
            id
            username
        }
        points
    }
}